version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12

    commands:
      - echo Installing dependencies...
      - npm install -g aws-cdk
      - pip install -r requirements.txt
      - pip install requests -t lambda/orchestrator

  build:
    commands:
      - echo Synthesizing CDK...
      - cdk synth

  post_build:
    commands:
      - echo Deploying CDK stacks...
      - cdk deploy HpChatbotLambdaStack --require-approval never

artifacts:
  files:
    - '**/*'